
- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{:class => "flash-message radius #{name.to_s == 'notice' ? 'success' : 'error'}", "data-alert" => ""}
      = content_tag :div, msg
      %a.close{:href => "#"} &times;