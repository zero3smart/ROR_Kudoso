= form_for [@family, @todo] do |f|
  - if @todo.errors.any?
    #error_explanation
      %h2= "#{pluralize(@todo.errors.count, "error")} prohibited this todo from being saved:"
      %ul
        - @todo.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_field :description
  %ul.small-block-grid-3
    %li
      = f.label :required
      = f.check_box :required
    %li
      = f.label :active
      = f.check_box :active
    %li
      = f.label :kudos
      = f.number_field :kudos
  .field
    = f.label :schedule
    = f.select_recurring :schedule
  .actions
    = f.submit 'Save'
