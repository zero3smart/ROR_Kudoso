%h1 Listing households
%table
  %thead
    %tr
      %th Name
      %th Primary contact
      %th{:colspan => "3"}
  %tbody
    - @households.each do |household|
      %tr
        %td= household.name
        %td= household.primary_contact_id
        %td= link_to 'Show', household
        %td= link_to 'Edit', edit_household_path(household)
        %td= link_to 'Destroy', household, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Household', new_household_path
