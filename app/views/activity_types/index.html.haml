%h1 Listing activity_types

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @activity_types.each do |activity_type|
    %tr
      %td= activity_type.name
      %td= link_to 'Show', activity_type
      %td= link_to 'Edit', edit_activity_type_path(activity_type)
      %td= link_to 'Destroy', activity_type, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Activity type', new_activity_type_path